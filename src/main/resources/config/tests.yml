tests:
  required-files: [general.yml, items.yml, attributes.yml, jewelry.yml, spaces.yml, commands.yml]
  lore-cases:
    - {line: "indierpg-id: abyss_ring", expected: abyss_ring}
    - {line: "mythic-id: Growth_Core", expected: growth_core}
  growth:
    unlimited-level: -1
    verify-different-per-level: true
